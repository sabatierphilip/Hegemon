export function SelfDestructConditions() {
  return <div className="rounded border border-border p-2 text-xs">Self-destruct checks compile into runtime _check_integrity() and _should_adapt().</div>;
}
