'use client';

import { Card } from '@/components/ui/card';

export default function SettingsPage() {
  return <Card className="p-3 text-xs">System configuration for API, key rotation, automation rails</Card>;
}
