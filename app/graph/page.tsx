'use client';

import { Card } from '@/components/ui/card';

export default function GraphPage() {
  return <Card className="p-3 text-xs">Topology & anomaly graph view (React Flow integration placeholder)</Card>;
}
